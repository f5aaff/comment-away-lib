# comment-away-lib

this library exists to load tree-sitter C shared objects in as languages, to generate parsers to be used in parsing source code.
In this case, namely for removing comments.

# Requirements

- git
- openssl
- openssl-devel

# building

- should be as easy as: ```cargo build```


